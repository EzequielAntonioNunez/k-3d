# Botole App - AI Agent Context
This file serves as the system instruction set for any AI working on this repository.

1. Always use Vanilla CSS with CSS variables from `tokens.css`.
2. Do not install Tailwind.
3. Keep 3D elements inside `src/components/3d` and pure UI inside `src/components/ui`.
4. Ensure 100% Lighthouse score practices.
